<p id="notice"><%= notice %></p>

<div class="jumbotron p-4 p-md-5 text-white rounded bg-dark">
  <div class="col-md-6 px-0">
    <h1 class="display-4 font-italic"><%= @first_blog.title %></h1>
    <p class="lead my-3"><%= @first_blog.body %></p>
    <p class="lead mb-0"><%= link_to 'Continue reading...', blog_path(@first_blog.id), class: "text-white font-weight-bold" %></p>
  </div>
</div>

<div class="row mb-2">
  <div class="col-md-6">
    <div class="row no-gutters border rounded overflow-hidden flex-md-row mb-4 shadow-sm h-md-250 position-relative">
      <div class="col p-4 d-flex flex-column position-static">
        <strong class="d-inline-block mb-2 text-primary"><%= @topic_three.topic.title %></strong>
        <h3 class="mb-0"><%= @topic_three.title %></h3>
        <div class="mb-1 text-muted">Published <%= distance_of_time_in_words(@topic_three.created_at, Time.now) %> ago</div>
        <p class="card-text mb-auto"><%= @topic_three.body %></p>
        <%= link_to 'Continue reading', blog_path(@topic_three.id), class: "stretched-link" %>
      </div>
      <div class="col-auto d-none d-lg-block">
        <p>INSERT THUMBNAIL HERE</p>
      </div>
    </div>
  </div>
  <div class="col-md-6">
    <div class="row no-gutters border rounded overflow-hidden flex-md-row mb-4 shadow-sm h-md-250 position-relative">
      <div class="col p-4 d-flex flex-column position-static">
        <strong class="d-inline-block mb-2 text-primary"><%= @topic_one.topic.title %></strong>
        <h3 class="mb-0"><%= @topic_one.title %></h3>
        <div class="mb-1 text-muted">Published <%= distance_of_time_in_words(@topic_one.created_at, Time.now) %> ago</div>
        <p class="card-text mb-auto"><%= @topic_one.body %></p>
        <%= link_to 'Continue reading', blog_path(@topic_one.id), class: "stretched-link" %>
      </div>
      <div class="col-auto d-none d-lg-block">
        <p>INSERT THUMBNAIL HERE</p>
      </div>
    </div>
  </div>
</div>

<div class="row">
  <main class="col-md-8 blog-main">
    <h3 class="pb-4 mb-4 font-italic border-bottom">
      From the Firehose
    </h3>
    <%= render @blogs %>
  </main><!-- /.blog-main -->

  <!-- Start of sidebar -->
  <%= render 'shared/blog_sidebar' %>
</div><!-- /.row -->